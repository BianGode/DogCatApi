What does my site need to do:

Call the Dogapi inside DogWrapper Component
Put the fetched data in an array
Loop through the array and load the Dog Component with loop element as image-prop source and random name as name-prop

Call the Catapi inside CatWrapper Component
Put the fetched data in an array
Loop through the array and load the Dog Component with loop element as image-prop source and random name as name-prop

When clicked on next page. An API call has to be made to load the next x images
Search for dog and cat species


Homepage
  Header
  DogWrapper
    Dog^x
  Catwrapper
    Cat^x
  Footer